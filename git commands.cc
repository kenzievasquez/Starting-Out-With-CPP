git remote set-url origin <url>
url (repository): git@github.com:kenzievasquez/cpp.git

ssh-keygen -t rsa -b 4096

git push --set-upstream origin master

Daily commands:
  git status
  git add -A
  git diff --cached
  git status
  git commit
  git push
  git pull
